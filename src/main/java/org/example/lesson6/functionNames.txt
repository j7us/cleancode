combine - combineLists
// Из задачи про связанные списки, статический мтод внутри отдельного класса, исправлено,
// чтобы название соответствовало формату void-функции "глагол + объект"

sort - sortList
// Из той же задачи, но метод связанный с сортировкой

processLspCall - sendRequestToLsp
// метод, который посылает запрос по rest, потом обрабатывает ответ с определенным набором логики.
// На основе ваших советов хочется разделить метод на несколько для отправки запроса и дальнейшей обработки ответа,
// и переназвать текущий метод

process - processRequestFromAut (processRequest)
// Есть класс который обрабатывает запрос из контроллера, но из название process не совсем понятно для чего этот метод используется

fromFeeInfoDto - buildFieldsContainerFromFeeInfo (getFieldsContainerFromFeeInfo, getFieldsContainer)
// Метод строит дто из нескольких найденных в бд entity, название смотрелось бы хорошо,
// если бы в аргументы передавались эти entity, а метод был бы в классе FieldsContainer,
// но он находится в сервис классе с отстраненным от этого названием

getAmounts - extractAmountsFromCancel
// Метод достает значения из поля amounts объекта Cancel, но т.к этот метод находится в отдельном от cancel классе,
// то необходимо добавить конкретики откуда берется этот amount

getCancelEndDate - getEndDate
// Функция находится внутри класса cancel, можно избавиться от упоминания объекта

getActionType - buildActionTypeByBusinessOperation
// Функция сторит объект ActionType исходя из данных в businessoperation

setTankerName - updateTankerName
// Функция обновляет имя танкера в зависимости от переданного в аргументы поля, но помимо операции set там лежит много логики,
// стоит разделить на несколько функций, а данную переназвать

buildReportQueryWithRestriction - buildReportQueryWithUserRestriction
// В приложении существовало несколько видов ограничений - для пользователей и для инспекторов,
// название функции может вводить в заблуждение без указания точного ограничения

move - startRace
// Из проекта старой игры про скачки, метод классе Hippodrome, который стартовал скачки

updateView - update
// Функция в классе отображения, можно избежать упоминания самого класса в функции