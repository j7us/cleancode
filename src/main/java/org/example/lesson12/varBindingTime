Attemp attempt = buildAttemptDependsOnType(event.getType);
// Переменная связывается с объектом во время получения определенного сообщения из кафки,
// т.к в зависимости от значения type attempt может заполняться по разному,
// был выбран способ связывания во время выполнения программы




@ConfigurationProperties
public class ApplicationProperties {
    String serviceName;
}
// В данном случае мы воспользовались связыванием во время выполнения, переменная связывается со значениям из файла
// application.yml




cancelInfo.setType(TT3970);
// Связывание во время компиляции применяется с помощью константы