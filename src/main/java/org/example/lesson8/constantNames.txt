plan - PLAN
// Найдена на просторах переданного команде проекта, поменял на заглавные буквы

endTime - END_OF_OPER_DAY
// Вынес одинаковые переменные endTime в константу, так как во всех случаях операционный день заканчивается в одно время

DEFAULT - DEFAULT_START_DAY
// Константа объявлена внутри класса, работающего с датами, но название не дает понимания для чего используется

WATER_SHIPMENT_REGISTRATION_NUMBER - WATER_SHIPMENT_REGISTRATION_NUMBER и SEA_SHIPMENT_REGISTRATION_NUMBER
// В константе сохранено значение номера регистрации для речного транспорта, но с тем же названием используется для морского,
// в остальных местах в приложении water и sea всегда разделены и не имеют общих переменных

TYPE_TWO - CARD_TYPE_PAY
// В константе сохранен номер типа действия с картой в виде значения long

SIX_PRIORITY - DEFAULT_PAYMENT_PRIORITY
// Константа содержит стандартный номер приоритета для платежа

DATE_TIME_FORMATTER
// Вынес в константу формат даты для форматирования

SCHEDULER_REPEAT_COUNT
// В разных местах приложения проставляется руками 3 попытки для определенного действия шедулера,
// заменил эти "магические числа" на константу

DEFAULT_PLAN_BATCH_AMOUNT
// Заменил стандартное значение, используемое в нескольких местах на константу

3818 - REPAY_EARLY_PAYMENT_TYPE
// Убрал использование числа из-за возможной пуаницы из-за названия

DEPOSIT_17 - DEPOSIT_FOR_EARLY_PAYMENT
// Убрал число из константы, т.к значение может измениться и название введет в заблуждение

TKS_DECLARATION
// Добавил константу для поля, которое в нескольких местах заполнялось одинаковой строкой